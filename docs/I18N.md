# Internacionalización (i18n)

## Estado

✅ `backend/lang/es/validation.php` completo con todos los mensajes de
   Laravel + atributos del dominio en español.
✅ `APP_LOCALE=es` por defecto en `.env.example`.
🔴 Multi-idioma EN del frontend con `next-intl` — **no cableado**.
🔴 Time zones por tenant — UI de cambio pendiente (campo `tenants.timezone`
   ya existe).
🔴 Validation messages EN si tenant pide servicio para clientes en USA.

## Backend Laravel

`lang/es/validation.php` traduce todos los mensajes de validación de
formularios. Cuando se llame `$request->validate(...)` y falle, los
errores vienen en español:

```json
{
  "errors": {
    "email": ["El campo email debe ser un correo electrónico válido."],
    "password": ["El campo contraseña es obligatorio."]
  }
}
```

Atributos amigables:

```php
'attributes' => [
    'email' => 'correo',
    'password' => 'contraseña',
    'starts_at' => 'fecha y hora',
    'service_id' => 'servicio',
    'barber_id' => 'barbero',
    // ...
],
```

Para añadir EN: copiar `lang/es/validation.php` a `lang/en/validation.php`
y traducir. `App::setLocale($lang)` cambia activo según preferencia del
usuario o header `Accept-Language`.

## Frontend Next.js

**No hay i18n cableada todavía.** Todos los strings están hardcoded en
español. Plan futuro:

1. `npm install next-intl`.
2. `i18n/request.ts` lee `Accept-Language` del request y elige `es`/`en`.
3. Mover strings a `messages/es.json` y `messages/en.json`.
4. `useTranslations()` hook en cada componente.
5. Toggle en navbar: ES/EN.

Estimación: ~3 días para 200 strings + plumbing.

## Catálogo de Locale

| Locale | Estado | Audiencia |
|--------|--------|-----------|
| es-MX  | ✅ default | LATAM core |
| en-US  | 🔴 pendiente | tenants en USA / Canadá |
| es-ES  | 🔴 futuro | España (variantes léxicas) |
| pt-BR  | 🔴 futuro | Brasil |

## Time zones por tenant

Campo `tenants.timezone` existe (default `America/Mexico_City`).
`AvailabilityCalculator` y `BookAppointment` ya respetan el timezone
del tenant cuando computan slots.

UI para cambiarlo: pendiente en `/admin/settings/locale` (no creado).

## Date / number formatting

Hoy hardcoded:

- `frontend/src/lib/utils.ts` — `fmtCents()` con `Intl.NumberFormat`,
  `fmtDateTime()` con `Intl.DateTimeFormat`. Locale `es-MX` por defecto.
- Cuando se cablee `next-intl`, cambiar a usar el locale del request.

## CFDI 4.0 idioma

CFDI requiere XML con códigos SAT específicos (sólo español, no
configurable). Separado del idioma del UI.
